[package]
name = "api"
version = "0.1.0"
edition = "2024"

[dependencies]
cuid2 = "0.1.4"
rocket = { version = "0.5.1", features = ["json"] }
serde = { version = "1.0.219", features = ["derive"] }
shared = { path = "../shared" }
